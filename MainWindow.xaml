<Window x:Class="FFmpegConverterGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FFmpeg Converter Tools GUI" Height="590" Width="806"
        MinHeight="590" MinWidth="806"
        MaxHeight="590" MaxWidth="806"
        Icon="icon.ico"
        Background="{DynamicResource WindowBrush}"
        Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        WindowStyle="None">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBrush" Color="#111318" />
        <SolidColorBrush x:Key="MenuBrush" Color="#0D0F14" />
        <SolidColorBrush x:Key="PanelBrush" Color="#181B22" />
        <SolidColorBrush x:Key="PanelAltBrush" Color="#20242D" />
        <SolidColorBrush x:Key="HoverBrush" Color="#2A303B" />
        <SolidColorBrush x:Key="PressedBrush" Color="#1F2530" />
        <SolidColorBrush x:Key="SelectedBrush" Color="#203F38" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#343A46" />
        <SolidColorBrush x:Key="TextBrush" Color="#F3F4F6" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#B6BECF" />
        <SolidColorBrush x:Key="DisabledTextBrush" Color="#6F7889" />
        <SolidColorBrush x:Key="AccentBrush" Color="#27D3A2" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#39E6B4" />
        <SolidColorBrush x:Key="AccentTextBrush" Color="#06120F" />
        <SolidColorBrush x:Key="HeaderOverlayBrush" Color="#660D0F14" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
            <Setter Property="Width" Value="12" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track"
                                   Orientation="{TemplateBinding Orientation}"
                                   IsDirectionReversed="False">
                                <Track.Thumb>
                                    <Thumb Background="{DynamicResource BorderBrushDark}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="True" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="12" />
                    <Setter Property="Height" Value="Auto" />
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="12" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    Margin="{TemplateBinding Padding}" />
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Row="0"
                                       Grid.Column="1"
                                       Orientation="Vertical"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Row="1"
                                       Grid.Column="0"
                                       Orientation="Horizontal"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                            <Border Grid.Row="1"
                                    Grid.Column="1"
                                    Background="{DynamicResource PanelAltBrush}"
                                    Visibility="{Binding Visibility, ElementName=PART_HorizontalScrollBar}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border CornerRadius="6"
                                Margin="2"
                                Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource MenuBrush}" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource MenuBrush}" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Grid>
                            <Border x:Name="HeaderBorder"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                            </Border>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource PanelAltBrush}"
                                        BorderBrush="{DynamicResource BorderBrushDark}"
                                        BorderThickness="1"
                                        Padding="4"
                                        MinWidth="190">
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle" />
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
                                <Setter Property="Background" Value="Transparent" />
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Right" />
                                <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuItem">
                                <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource HoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledTextBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Height="1"
                                Margin="6,4"
                                Background="{DynamicResource BorderBrushDark}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ComboBorder"
                                                CornerRadius="8"
                                                Background="{DynamicResource PanelAltBrush}"
                                                BorderBrush="{DynamicResource BorderBrushDark}"
                                                BorderThickness="1"
                                                Padding="{TemplateBinding Padding}"
                                                TextElement.Foreground="{DynamicResource TextBrush}">
                                            <Grid>
                                                <ContentPresenter Margin="2,0,28,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"
                                                                  RecognizesAccessKey="True"
                                                                  TextElement.Foreground="{DynamicResource TextBrush}"
                                                                  Content="{Binding Path=SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                                                <Path x:Name="Arrow"
                                                      HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,7,0"
                                                      Fill="{DynamicResource MutedTextBrush}"
                                                      Data="M 0 0 L 4 4 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ComboBorder" Property="Background" Value="{DynamicResource HoverBrush}" />
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
                                                <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource TextBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource PanelAltBrush}"
                                        BorderBrush="{DynamicResource BorderBrushDark}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,4,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="260">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelAltBrush}" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SelectedBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource HoverBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.48" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                    <Setter Property="Background" Value="{DynamicResource BorderBrushDark}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                    <Setter Property="Background" Value="{DynamicResource PressedBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="Box"
                                    Width="16"
                                    Height="16"
                                    CornerRadius="4"
                                    Background="{DynamicResource PanelAltBrush}"
                                    BorderBrush="{DynamicResource BorderBrushDark}"
                                    BorderThickness="1">
                                <Border x:Name="Mark"
                                        Width="8"
                                        Height="8"
                                        CornerRadius="2"
                                        Background="{DynamicResource AccentBrush}"
                                        Opacity="0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                            </Border>
                            <ContentPresenter Margin="7,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Mark" Property="Opacity" Value="1" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                    <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TitleBarButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBrush}" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PressedBrush}" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel Margin="0">
        <Border DockPanel.Dock="Top"
                Height="30"
                Background="{DynamicResource WindowBrush}"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Image Source="icon.ico" Width="16" Height="16" Margin="0,0,8,0" />
                    <TextBlock Text="FFmpeg Converter Tools GUI" VerticalAlignment="Center" FontSize="12" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Name="MinimizeTitleButton" Content="_" Style="{StaticResource TitleBarButton}" Click="MinimizeTitleButton_Click" />
                    <Button Name="CloseTitleButton" Content="X" Style="{StaticResource TitleBarButton}" Click="CloseTitleButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Height="76" DockPanel.Dock="Top" ClipToBounds="True" Background="{DynamicResource MenuBrush}">
            <Grid>
                <Image Source="banner-home.png"
                       Height="76"
                       Width="806"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top" />
                <Menu VerticalAlignment="Top" Background="Transparent">
                    <MenuItem Header="_File">
                        <MenuItem Header="_Open files..." Click="OpenFilesMenuItem_Click" />
                        <MenuItem Header="_Clear queue" Click="ClearQueueMenuItem_Click" />
                        <Separator />
                        <MenuItem Header="E_xit" Click="ExitMenuItem_Click" />
                    </MenuItem>
                    <MenuItem Header="_Settings">
                        <MenuItem Header="_Theme">
                            <MenuItem Name="DarkThemeMenuItem" Header="_Dark" IsCheckable="True" IsChecked="True" Click="DarkThemeMenuItem_Click" />
                            <MenuItem Name="LightThemeMenuItem" Header="_Light" IsCheckable="True" Click="LightThemeMenuItem_Click" />
                        </MenuItem>
                    </MenuItem>
                    <MenuItem Header="_Help">
                        <MenuItem Header="_About" Click="AboutMenuItem_Click" />
                    </MenuItem>
                </Menu>
            </Grid>
        </Border>

        <DockPanel Margin="12,6,12,12">

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" DockPanel.Dock="Top">
            <TextBlock Text="Tool:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Name="ToolSelector" Width="150" SelectedIndex="0">
                <ComboBoxItem>Compress</ComboBoxItem>
                <ComboBoxItem>Convert</ComboBoxItem>
                <ComboBoxItem>Cut</ComboBoxItem>
                <ComboBoxItem>Merge</ComboBoxItem>
                <ComboBoxItem>Remux / Repair</ComboBoxItem>
                <ComboBoxItem>Remove Subtitles</ComboBoxItem>
            </ComboBox>
            <StackPanel Name="CompressParams" Orientation="Horizontal" Margin="12,0,0,0">
                <TextBlock Text="Quality:" VerticalAlignment="Center" Margin="6,0,4,0" />
                <ComboBox Name="QualitySelector" Width="90" SelectedIndex="0">
                    <ComboBoxItem>100%</ComboBoxItem>
                    <ComboBoxItem>90%</ComboBoxItem>
                    <ComboBoxItem>80%</ComboBoxItem>
                    <ComboBoxItem>70%</ComboBoxItem>
                    <ComboBoxItem>60%</ComboBoxItem>
                    <ComboBoxItem>50%</ComboBoxItem>
                    <ComboBoxItem>40%</ComboBoxItem>
                    <ComboBoxItem>30%</ComboBoxItem>
                    <ComboBoxItem>20%</ComboBoxItem>
                    <ComboBoxItem>10%</ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel Name="ConvertParams" Orientation="Horizontal" Margin="12,0,0,0" Visibility="Collapsed">
                <TextBlock Text="Type:" VerticalAlignment="Center" Margin="6,0,4,0" />
                <ComboBox Name="ConvertTypeSelector" Width="80" SelectedIndex="0">
                    <ComboBoxItem>Video</ComboBoxItem>
                    <ComboBoxItem>Audio</ComboBoxItem>
                    <ComboBoxItem>Image</ComboBoxItem>
                </ComboBox>
                <TextBlock Text="Format:" VerticalAlignment="Center" Margin="8,0,4,0" />
                <ComboBox Name="FormatSelector" Width="90" SelectedIndex="0">
                    <ComboBoxItem>MP4</ComboBoxItem>
                    <ComboBoxItem>MKV</ComboBoxItem>
                    <ComboBoxItem>AVI</ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel Name="CutParams" Orientation="Horizontal" Margin="12,0,0,0" Visibility="Collapsed">
                <TextBlock Text="Type:" VerticalAlignment="Center" Margin="6,0,4,0" />
                <ComboBox Name="CutTypeSelector" Width="80" SelectedIndex="0">
                    <ComboBoxItem>Video</ComboBoxItem>
                    <ComboBoxItem>Audio</ComboBoxItem>
                </ComboBox>
                <TextBlock Text="Start:" VerticalAlignment="Center" Margin="8,0,4,0" />
                <TextBox Name="StartTextBox" Width="105" Text="00:00:00.000" />
                <TextBlock Text="End:" VerticalAlignment="Center" Margin="6,0,4,0" />
                <TextBox Name="EndTextBox" Width="105" Text="00:00:00.000" />
            </StackPanel>
            <CheckBox Name="ReplaceOriginalCheckBox"
                      Content="Replace original"
                      VerticalAlignment="Center"
                      Margin="12,0,0,0"
                      IsChecked="False"
                      Foreground="{DynamicResource TextBrush}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8" DockPanel.Dock="Bottom">
            <Button Name="AddButton" Content="Add" Width="90" Margin="4" />
            <Button Name="ClearButton" Content="Clear" Width="80" Margin="4" />
            <Button Name="CancelButton" Content="Cancel" Width="90" Margin="4" IsEnabled="False" />
            <Button Name="ProcessButton" Content="Process" Width="110" Margin="4" Style="{StaticResource PrimaryButton}" />
        </StackPanel>

        <ProgressBar Name="JobProgressBar" DockPanel.Dock="Bottom" Height="8" Margin="0,8,0,0" Minimum="0" Maximum="100" Value="0" Foreground="{DynamicResource AccentBrush}" Background="{DynamicResource PanelAltBrush}" />

        <TextBox Name="LogTextBox" DockPanel.Dock="Bottom" Height="105" Margin="0,0,0,0" IsReadOnly="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />

        <Grid DockPanel.Dock="Bottom" Margin="0,6,0,4">
            <TextBlock Text="Log" FontWeight="Bold" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" />
            <CheckBox Name="AutoScrollCheckBox"
                      Content="Autoscroll"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Center"
                      IsChecked="True"
                      Foreground="{DynamicResource TextBrush}" />
        </Grid>

        <Border Name="DropArea" BorderBrush="{DynamicResource BorderBrushDark}" BorderThickness="2" Background="{DynamicResource PanelBrush}" Padding="10" AllowDrop="True">
            <StackPanel>
                <TextBlock Text="Drop files here or click Add" Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,6" />
                <ListBox Name="FilesList" MinHeight="180" />
            </StackPanel>
        </Border>
        </DockPanel>
    </DockPanel>
</Window>

